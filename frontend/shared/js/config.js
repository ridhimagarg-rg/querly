// Optional deployment configuration. Set window.QUERLY_API_BASE before api.js,
// or provide apiBase in window.QUERLY_CONFIG from a host-specific config file.
window.QUERLY_CONFIG = window.QUERLY_CONFIG || {};
// Example: window.QUERLY_CONFIG.apiBase = "https://api.example.com/api";
